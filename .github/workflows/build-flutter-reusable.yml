name: Build Flutter APK (Reusable)

on:
  workflow_call:
    inputs:
      jobId:
        required: true
        type: string
      userId:
        required: true
        type: string
      payload:
        required: true
        type: string

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 27

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Download Project ZIP
        run: |
          URL=$(echo '${{ inputs.payload }}' | jq -r '.zipAssetUrl')
          curl -L -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" -H "Accept: application/octet-stream" -o project.zip "$URL"
          mkdir -p project
          unzip -qq project.zip -d project
          PUBSPEC=$(find project -maxdepth 8 -iname "pubspec.yaml" | head -1)
          if [ -z "$PUBSPEC" ]; then
            echo "pubspec.yaml tidak ditemukan di dalam ZIP"
            exit 1
          fi
          PROJECT_ROOT=$(dirname "$PUBSPEC")
          echo "PROJECT_ROOT=$PROJECT_ROOT" >> $GITHUB_ENV

      - name: Clean Stale Build Cache
        run: |
          cd "${{ env.PROJECT_ROOT }}"
          rm -rf build .dart_tool android/.gradle android/build android/app/build ios/Pods ios/.symlinks
          find . -iname "local.properties" -delete

      - name: Fix Android SDK Path
        run: echo "sdk.dir=$ANDROID_HOME" > "${{ env.PROJECT_ROOT }}/android/local.properties"

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Install NDK and Build Tools
        run: |
          echo "y" | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager \
            "ndk;27.0.12077973" \
            "ndk;28.2.13676358" \
            "build-tools;35.0.0" \
            --sdk_root=$ANDROID_HOME 2>&1 | tail -3 || true

      - name: Detect Required Flutter Version
        run: |
          if grep -A3 '^environment:' "${{ env.PROJECT_ROOT }}/pubspec.yaml" | grep -qE '^\s*flutter:\s*[''"]?[0-9]'; then
            echo "HAS_FLUTTER_VERSION=true" >> $GITHUB_ENV
          else
            echo "HAS_FLUTTER_VERSION=false" >> $GITHUB_ENV

            DART_MIN=$(grep -A3 '^environment:' "${{ env.PROJECT_ROOT }}/pubspec.yaml" | grep -oE "sdk:\s*['\"]?>=[0-9]+\.[0-9]+" | grep -oE "[0-9]+\.[0-9]+" | head -1)
            echo "Terdeteksi Dart SDK minimum: $DART_MIN"

            INFERRED_FLUTTER=$(python3 - "$DART_MIN" <<'PYEOF'
          import sys

          dart_min = sys.argv[1] if len(sys.argv) > 1 else ''
          table = [
              ('3.9', '3.35.0'), ('3.8', '3.32.0'), ('3.7', '3.29.0'), ('3.6', '3.27.0'),
              ('3.5', '3.24.0'), ('3.4', '3.22.0'), ('3.3', '3.19.0'), ('3.2', '3.16.0'),
              ('3.1', '3.13.0'), ('3.0', '3.10.0'),
          ]
          if not dart_min:
              print('3.24.5')
          else:
              try:
                  major, minor = map(int, dart_min.split('.'))
                  result = '3.10.0'
                  for d_ver, f_ver in table:
                      d_major, d_minor = map(int, d_ver.split('.'))
                      if (major, minor) >= (d_major, d_minor):
                          result = f_ver
                          break
                  print(result)
              except Exception:
                  print('3.24.5')
          PYEOF
          )
            echo "Flutter yang dipakai (inferred dari Dart SDK): $INFERRED_FLUTTER"
            echo "INFERRED_FLUTTER_VERSION=$INFERRED_FLUTTER" >> $GITHUB_ENV
          fi

      - name: Setup Flutter (versi sesuai project)
        if: env.HAS_FLUTTER_VERSION == 'true'
        uses: subosito/flutter-action@v2
        with:
          flutter-version-file: ${{ env.PROJECT_ROOT }}/pubspec.yaml
          channel: stable
          cache: true

      - name: Setup Flutter (versi hasil inferensi dari Dart SDK project)
        if: env.HAS_FLUTTER_VERSION == 'false'
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.INFERRED_FLUTTER_VERSION }}
          channel: stable
          cache: true

      - name: Flutter Pub Get (attempt 1)
        id: pub_get_1
        working-directory: ${{ env.PROJECT_ROOT }}
        continue-on-error: true
        run: |
          flutter pub get 2>&1 | tee /tmp/pub_get_1.log
          exit ${PIPESTATUS[0]}

      - name: Deteksi Saran Versi Flutter dari Pub
        if: steps.pub_get_1.outcome == 'failure'
        run: |
          SUGGESTED=$(grep -oE "Try using the Flutter SDK version: [0-9]+\.[0-9]+\.[0-9]+" /tmp/pub_get_1.log | grep -oE "[0-9]+\.[0-9]+\.[0-9]+" | head -1)
          if [ -n "$SUGGESTED" ]; then
            echo "Pub menyarankan Flutter versi: $SUGGESTED"
            echo "SUGGESTED_FLUTTER_VERSION=$SUGGESTED" >> $GITHUB_ENV
          else
            echo "Tidak ada saran versi spesifik dari pub, gagal murni."
          fi

      - name: Setup Flutter (ganti ke versi saran pub)
        if: steps.pub_get_1.outcome == 'failure' && env.SUGGESTED_FLUTTER_VERSION != ''
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.SUGGESTED_FLUTTER_VERSION }}
          channel: stable
          cache: true

      - name: Flutter Pub Get (attempt 2, setelah ganti versi)
        if: steps.pub_get_1.outcome == 'failure' && env.SUGGESTED_FLUTTER_VERSION != ''
        working-directory: ${{ env.PROJECT_ROOT }}
        run: flutter pub get

      - name: Gagalkan Job Jika Pub Get Tetap Gagal
        if: steps.pub_get_1.outcome == 'failure' && env.SUGGESTED_FLUTTER_VERSION == ''
        run: |
          echo "flutter pub get gagal dan tidak ada saran versi Flutter yang bisa dicoba."
          exit 1

      - name: Auto-Detect & Install Required Android SDK Platform
        working-directory: ${{ env.PROJECT_ROOT }}
        run: |
          PROJECT_COMPILE_SDK=0
          GRADLE_FILE=""
          for f in "android/app/build.gradle.kts" "android/app/build.gradle"; do
            if [ -f "$f" ]; then
              SDK=$(grep -E "compileSdk(Version)?\s*[=\s]\s*[0-9]+" "$f" 2>/dev/null \
                    | grep -oE '\b[0-9]{2}\b' | sort -n | tail -1)
              if [ -n "$SDK" ]; then
                PROJECT_COMPILE_SDK=$SDK
                GRADLE_FILE="$f"
              fi
              break
            fi
          done
          echo "Project compileSdk saat ini: $PROJECT_COMPILE_SDK (dari ${GRADLE_FILE:-tidak ditemukan})"

          PLUGIN_COMPILE_SDK=$(find ~/.pub-cache -type f \( -name "build.gradle" -o -name "build.gradle.kts" \) \
            -path "*/android/*" 2>/dev/null \
            | xargs grep -h -E "compileSdk(Version)?\s*[=\s]\s*[0-9]+" 2>/dev/null \
            | grep -oE '\b[0-9]{2}\b' | sort -n | tail -1 || echo "0")
          echo "Plugin tertinggi compileSdk yang ditemukan: $PLUGIN_COMPILE_SDK"

          REQUIRED_SDK=$PROJECT_COMPILE_SDK
          if [ -n "$PLUGIN_COMPILE_SDK" ] && [ "$PLUGIN_COMPILE_SDK" -gt "$REQUIRED_SDK" ]; then
            REQUIRED_SDK=$PLUGIN_COMPILE_SDK
          fi

          if [ -z "$REQUIRED_SDK" ] || [ "$REQUIRED_SDK" -lt 33 ]; then
            REQUIRED_SDK=35
          fi

          echo "Required Android SDK Platform yang akan diinstall: android-$REQUIRED_SDK"
          echo "DETECTED_COMPILE_SDK=$REQUIRED_SDK" >> $GITHUB_ENV

          if [ -n "$GRADLE_FILE" ] && [ "$REQUIRED_SDK" -gt "$PROJECT_COMPILE_SDK" ]; then
            echo "Auto-patching $GRADLE_FILE: compileSdk $PROJECT_COMPILE_SDK → $REQUIRED_SDK"
            sed -i "s/compileSdk\s*=\s*$PROJECT_COMPILE_SDK/compileSdk = $REQUIRED_SDK/" "$GRADLE_FILE"
            sed -i "s/targetSdk\s*=\s*$PROJECT_COMPILE_SDK/targetSdk = $REQUIRED_SDK/" "$GRADLE_FILE" || true
            echo "Hasil patch $GRADLE_FILE:"
            grep -E "compileSdk|targetSdk" "$GRADLE_FILE"
          else
            echo "Tidak perlu patch — project compileSdk sudah memenuhi requirement."
          fi

          echo "Installing platforms;android-$REQUIRED_SDK ..."
          echo "y" | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager \
            "platforms;android-$REQUIRED_SDK" \
            --sdk_root=$ANDROID_HOME 2>&1 | tail -5 || true

          if [ "$REQUIRED_SDK" != "35" ]; then
            echo "y" | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager \
              "platforms;android-35" \
              --sdk_root=$ANDROID_HOME 2>&1 | tail -3 || true
          fi

      - name: Tune JVM Memory untuk Gradle
        working-directory: ${{ env.PROJECT_ROOT }}
        run: |
          mkdir -p android
          touch android/gradle.properties
          grep -q "org.gradle.jvmargs" android/gradle.properties || echo "org.gradle.jvmargs=-Xmx3072m -XX:MaxMetaspaceSize=1024m -XX:+HeapDumpOnOutOfMemoryError" >> android/gradle.properties
          grep -q "org.gradle.daemon" android/gradle.properties || echo "org.gradle.daemon=true" >> android/gradle.properties
          cat android/gradle.properties

      - name: Build APK Release
        working-directory: ${{ env.PROJECT_ROOT }}
        timeout-minutes: 25
        run: |
          set +e
          BUILD_LOG_FILE=$(mktemp)
          flutter build apk --release --no-tree-shake-icons 2>&1 | tee "$BUILD_LOG_FILE"
          BUILD_EXIT=${PIPESTATUS[0]}

          if [ $BUILD_EXIT -ne 0 ]; then
            REQUIRED_SDK=$(grep -oE "cannot be smaller than version [0-9]+" "$BUILD_LOG_FILE" | grep -oE "[0-9]+" | head -1)

            if [ -n "$REQUIRED_SDK" ]; then
              echo ""
              echo "Terdeteksi butuh minSdk minimal $REQUIRED_SDK, auto-patch build.gradle dan build ulang..."
              python3 - "$REQUIRED_SDK" <<'PYEOF'
          import re, os, sys

          required = sys.argv[1]
          candidates = ["android/app/build.gradle.kts", "android/app/build.gradle"]

          for path in candidates:
              if os.path.exists(path):
                  with open(path) as f:
                      content = f.read()
                  new_content = re.sub(
                      r'(minSdk(?:Version)?\s*=?\s*)(flutter\.minSdkVersion|\d+)',
                      lambda m: m.group(1) + required,
                      content
                  )
                  if new_content != content:
                      with open(path, 'w') as f:
                          f.write(new_content)
                      print(f"Patched {path}: minSdk -> {required}")
                  else:
                      print(f"Tidak menemukan baris minSdk di {path} untuk di-patch")
                  break
          else:
              print("Tidak menemukan file build.gradle/build.gradle.kts")
          PYEOF

              flutter build apk --release --no-tree-shake-icons 2>&1 | tee "$BUILD_LOG_FILE"
              BUILD_EXIT=${PIPESTATUS[0]}
            fi
          fi

          if [ $BUILD_EXIT -ne 0 ]; then
            MISSING_PLATFORM=$(grep -oE "Failed to find Platform SDK with path: platforms;android-[0-9]+" "$BUILD_LOG_FILE" \
              | grep -oE "android-[0-9]+" | grep -oE "[0-9]+" | head -1)

            if [ -n "$MISSING_PLATFORM" ]; then
              echo ""
              echo "Terdeteksi missing platforms;android-$MISSING_PLATFORM, install dan build ulang..."
              echo "y" | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager \
                "platforms;android-$MISSING_PLATFORM" \
                --sdk_root=$ANDROID_HOME 2>&1 | tail -5

              for gradle_f in "android/app/build.gradle.kts" "android/app/build.gradle"; do
                if [ -f "$gradle_f" ]; then
                  CUR_SDK=$(grep -E "compileSdk(Version)?\s*[=\s]\s*[0-9]+" "$gradle_f" 2>/dev/null \
                            | grep -oE '\b[0-9]{2}\b' | sort -n | tail -1 || echo "0")
                  if [ -n "$CUR_SDK" ] && [ "$MISSING_PLATFORM" -gt "$CUR_SDK" ]; then
                    echo "Patching $gradle_f: compileSdk $CUR_SDK → $MISSING_PLATFORM"
                    sed -i "s/compileSdk\s*=\s*$CUR_SDK/compileSdk = $MISSING_PLATFORM/" "$gradle_f"
                    sed -i "s/targetSdk\s*=\s*$CUR_SDK/targetSdk = $MISSING_PLATFORM/" "$gradle_f" || true
                  fi
                  break
                fi
              done

              flutter build apk --release --no-tree-shake-icons 2>&1 | tee "$BUILD_LOG_FILE"
              BUILD_EXIT=${PIPESTATUS[0]}
            fi
          fi

          if [ $BUILD_EXIT -ne 0 ]; then
            INCOMPAT_ERROR=$(grep -cE "can't be (extended|implemented|mixed in) outside of its library|Error: The (method|getter|setter|class|argument) .* (isn't defined|can't be)" "$BUILD_LOG_FILE" || true)

            if [ "$INCOMPAT_ERROR" -gt 0 ]; then
              echo ""
              echo "Terdeteksi kemungkinan incompatibility versi package dengan Flutter SDK, mencoba flutter pub upgrade dan build ulang..."
              flutter pub upgrade
              flutter build apk --release --no-tree-shake-icons 2>&1 | tee "$BUILD_LOG_FILE"
              BUILD_EXIT=${PIPESTATUS[0]}
            fi
          fi

          if [ $BUILD_EXIT -ne 0 ]; then
            INCOMPAT_ERROR_2=$(grep -cE "can't be (extended|implemented|mixed in) outside of its library|Error: The (method|getter|setter|class|argument) .* (isn't defined|can't be)" "$BUILD_LOG_FILE" || true)

            if [ "$INCOMPAT_ERROR_2" -gt 0 ]; then
              echo ""
              echo "Masih incompatible setelah pub upgrade biasa. Mencoba flutter pub upgrade --major-versions (bisa lompat versi major)..."
              flutter pub upgrade --major-versions
              flutter build apk --release --no-tree-shake-icons
              BUILD_EXIT=$?
            fi
          fi

          if [ $BUILD_EXIT -ne 0 ]; then
            OFFENDING_PLUGIN=$(grep -oE "The plugin [a-zA-Z0-9_]+ requires Android SDK version [0-9]+" "$BUILD_LOG_FILE" | grep -oE "^The plugin [a-zA-Z0-9_]+" | grep -oE "[a-zA-Z0-9_]+$" | head -1)

            if [ -n "$OFFENDING_PLUGIN" ]; then
              echo ""
              echo "Plugin $OFFENDING_PLUGIN butuh Android SDK yang belum stable/tersedia. Coba downgrade plugin ini dan build ulang..."
              flutter pub downgrade "$OFFENDING_PLUGIN"
              flutter build apk --release --no-tree-shake-icons
              BUILD_EXIT=$?
            fi
          fi

          exit $BUILD_EXIT

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: apk-${{ inputs.jobId }}
          path: ${{ env.PROJECT_ROOT }}/build/app/outputs/flutter-apk/app-release.apk
          retention-days: 7
